#include <stdlib.h>
#include <stdio.h>

int main() {
    char name[40];
    char address[255];
puts(name);
puts(address);
    printf("Enter your name: ");
    scanf("%39s", name);
    puts(name);
    puts(address);
    return 0;
}
